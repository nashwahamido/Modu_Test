// Expo Metro config, extended to allow importing .glb files (needed by
// react-native-filament). Safe for the existing app — it only adds an
// asset extension and changes nothing else.
const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

config.resolver.assetExts.push('glb');

module.exports = config;